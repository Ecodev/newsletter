<phpunit
    bootstrap="UnitTestsBootstrap.php"
    colors="true"
>

    <testsuites>
        <testsuite name="EXT:newsletter unit tests">
            <directory>../Unit/</directory>
        </testsuite>
    </testsuites>

    <filter>
        <whitelist>
            <!-- Include only our own code for code coverage statistics -->
            <directory>../../Classes</directory>
        </whitelist>
    </filter>
</phpunit>
