Index: Classes/MVC/Web/Routing/URIBuilder.php
===================================================================
--- Classes/MVC/Web/Routing/URIBuilder.php	(revision 1123)
+++ Classes/MVC/Web/Routing/URIBuilder.php	(working copy)
@@ -158,8 +158,18 @@
 		if ($linkAccessRestrictedPages) {
 			$typolinkConfiguration['linkAccessRestrictedPages'] = 1;
 		}
-
-		return ($absolute === TRUE ? $this->request->getBaseURI() : '') . $this->contentObject->typoLink_URL($typolinkConfiguration);
+		
+		if (TYPO3_MODE === 'FE') {
+			return ($absolute === TRUE ? $this->request->getBaseURI() : '') . $this->contentObject->typoLink_URL($typolinkConfiguration);
+		} else { // TYPO3_MODE === 'BE'
+			$additionalParams = t3lib_div::trimExplode('&', $typolinkConfiguration['additionalParams'], TRUE);
+			$parameters = array();
+			foreach ($additionalParams as $params) {
+				list($key, $value) = explode('=', $params);
+				$parameters[$key] = $value;
+			}
+			return t3lib_div::linkThisUrl($typolinkConfiguration['parameter'], $parameters);
+		}
 	}
 }
-?>
\ No newline at end of file
+?>
